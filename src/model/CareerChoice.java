package model;
/**
 * CareerChoice class, a class under MagentaSpace, this class indicates all the state and behaviour of the CareerChoice Space.
 */
public class CareerChoice extends MagentaSpace
{
    /**
     * This is an CareerChoice standard constructor that accepts a String as a name that is to be assigned to the CareerChoice.
     *
     * @param type the name of the CareerChoice to be assigned, also the name is inherited from MagentaSpace class.
     */
    public CareerChoice(String type)
    {
        super(type);
    }
}